<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Thank You</title>
  <link
    href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    rel="stylesheet"
  />
  <script>
    setTimeout(() => {
      window.location.href = "<%= redirectUrl %>";
    }, 3000);
  </script>
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen">
  <div class="text-center bg-white p-10 rounded-2xl shadow-lg max-w-md">
    <h1 class="text-3xl font-bold text-green-600 mb-4">
       Thank You, <%= user?.name || "Candidate" %>!
    </h1>
    <p class="text-gray-700 mb-6">
      Your test has been submitted successfully.<br />
      You will be redirected to your <strong>Status</strong> page shortly.
    </p>

    <a
      href="<%= redirectUrl %>"
      class="bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-full transition"
    >
      Go to Status Now
    </a>
  </div>
</body>
</html>

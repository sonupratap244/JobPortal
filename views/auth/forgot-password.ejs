<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Forgot Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-100 via-indigo-50 to-purple-100 p-4">

  <div class="bg-white w-full max-w-full sm:max-w-md md:max-w-lg p-8 rounded-2xl shadow-xl transition-transform duration-300 hover:-translate-y-1">
    <h2 class="text-2xl sm:text-3xl font-bold text-center text-indigo-700 mb-6">Forgot Password</h2>

    <% if (error) { %>
      <div class="text-center text-sm sm:text-base text-red-600 bg-red-50 border border-red-200 rounded-lg py-2 mb-4">
        <%= error %>
      </div>
    <% } %>

    <% if (success) { %>
      <div class="text-center text-sm sm:text-base text-green-600 bg-green-50 border border-green-200 rounded-lg py-2 mb-4">
        <%= success %>
      </div>
    <% } %>

    <form method="POST" action="/auth/forgot-password" class="space-y-5">
      <div>
        <input 
          type="text" 
          name="identifier" 
          placeholder="Enter Email or Mobile" 
          required
          class="w-full px-4 py-3 border border-gray-300 rounded-lg text-gray-700 text-sm sm:text-base focus:outline-none focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 transition"
        >
      </div>
      <button 
        type="submit"
        class="w-full py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-lg text-sm sm:text-base shadow-md transition">
        Send Reset Link
      </button>
    </form>

    <a 
      href="/auth/login" 
      class="block text-center mt-5 text-sm sm:text-base text-indigo-700 hover:text-indigo-800 hover:underline transition">
      ← Back to Login
    </a>
  </div>

</body>
</html>
